# *مهم جداً*: استبدل your_domain.com بالنطاق الذي قمت بربطه في الخطوة السابقة.
mof-yemen.info {
    
    # يقوم Caddy تلقائيًا بطلب شهادة SSL مجانية (Let's Encrypt)
    
    # توجيه جميع طلبات HTTPS/HTTP إلى حاوية Next.js الداخلية
    # نستخدم اسم الخدمة (nextjs-app) والمنفذ الداخلي (3000)
    reverse_proxy nextjs-app:3000
    
    # تفعيل ضغط البيانات (اختياري، لكنه يحسن الأداء)
    encode gzip
}